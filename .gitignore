node_modules
.next
out
.env*.local
.env
!.env.example
*.log
.DS_Store
.vercel
*.tsbuildinfo
next-env.d.ts
*.xlsx
*.pptx

# Datos derivados de la investigación comercial. El repo es público: las fotos
# son de personas identificables y el contenido del PPT es material interno.
# Las fotos viven en Supabase Storage, que es el sistema de registro real; acá
# se regeneran con scripts/extraer_fotos_mexico.py a partir del PPT original.
scripts/fotos_mexico/
extracted_content.txt
.claude/settings.local.json
scripts/seed-representantes.ts

# Snapshots de DB generados por backup-snapshot.ts — son data, no código
backups/
